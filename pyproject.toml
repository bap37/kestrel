[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "kestrel"
version = "0.1.0"
description = "SBI framework for Type Ia supernova dust population inference"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
